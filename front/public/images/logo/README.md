# Logo — poné tu archivo acá

Nombre esperado: `sugarcoach-logo.png`
(también vale `sugarcoach-logo.webp` o `logo.svg` — avisame y ajusto `LOGO_SRC`).

Ruta final: `public/images/logo/sugarcoach-logo.png` → URL `/images/logo/sugarcoach-logo.png`.

Mientras tanto el sitio usa el logo remoto de la landing de ejemplo
(ver `LOGO_SRC` en `lib/images.ts`).
